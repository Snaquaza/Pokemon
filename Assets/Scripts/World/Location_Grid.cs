using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Location_Grid : MonoBehaviour {

	public Grid[,] grid = new Grid[64, 64];
    
	// Use this for initialization
	void Start () {
		// create grid
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
